import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.carlosdonosopheva2',
  appName: 'carlosdonosopheva2',
  webDir: 'www'
};

export default config;
